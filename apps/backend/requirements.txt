# Core dependencies
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
numpy>=1.26.0
pandas>=2.1.0
python-dateutil>=2.8.0

# Testing
pytest>=7.4.0
pytest-xprocess>=0.23.0
pytest-asyncio>=0.21.0

# Database
pymongo>=4.6.0
motor>=3.3.0

# Sponsor integrations (install as needed)
# Note: These are optional sidecars - core system works without them
# LeanMCP - Primary
mcp>=0.1.0

# Kairo - Primary (Solana)
# Note: anchorpy requires pytest-xprocess for its pytest plugin
solana>=0.30.0
anchorpy>=0.18.0

# Arize - Secondary (optional)
arize>=7.0.0

# Browserbase - Secondary (optional)
browserbase>=0.1.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.26.0
