{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "description": "SATOR Ops Backend - FastAPI Python Server",
  "scripts": {
    "dev": "python3 main.py",
    "start": "python3 main.py",
    "dev:uvicorn": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "test": "python3 -m pytest tests/",
    "lint": "python3 -m flake8 app/",
    "clean": "find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true"
  }
}
